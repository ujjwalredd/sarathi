Repair `signed_cookies.py` so it implements the documented version 1 signed cookie format.

`sign_cookie(value, key, *, key_id, issued_at)` must return `v1.<key_id>.<issued_at>.<payload>.<signature>`. The value is UTF-8 encoded and then URL-safe base64 encoded without padding. The signature is lowercase HMAC-SHA256 hex over the ASCII text `v1.<key_id>.<issued_at>.<payload>`. `value` and `key_id` must be strings, the key must be nonempty bytes, and `issued_at` must be a nonnegative integer but not a bool. A key ID must be 1 to 32 ASCII letters, digits, underscores, or hyphens.

`verify_cookie(token, keys, *, max_age, now)` must validate the exact five-field format, select the key by key ID so current and previous keys can coexist, verify the signature with a timing-safe comparison, enforce canonical unpadded base64 and valid UTF-8, and return the original string. `max_age` and `now` must be nonnegative integers but not bools. A cookie is valid when `0 <= now - issued_at <= max_age`; future and expired cookies are invalid. Reject unknown key IDs, malformed fields, and invalid signatures with `CookieError`. Public argument type or range mistakes must raise `TypeError` or `ValueError` as appropriate. Do not mutate the key mapping.
